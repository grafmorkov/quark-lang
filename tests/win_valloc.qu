load "std::io";
load "std::heap";

i32 main() {
    *void p = std::heap::heap_malloc(16);
    if (p == (0 as! *void)) {
        std::io::print("ALLOC FAILED\n");
        return 1;
    }
    std::io::print("ALLOC OK\n");
    return 0;
}
