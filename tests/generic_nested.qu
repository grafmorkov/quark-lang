load "std::io";
load "std::vector";
using std::vector;

struct Token {
    i32 id;
};

void main() {
    @init mut Vec<Vec<Token>> tokens;

    vec_free<Vec<Token>>(tokens);
    std::io::print("ok\n");
}
